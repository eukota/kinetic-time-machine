name: CI

on:
  push:
    branches: [main, staging]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend (Docker + pytest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          tags: kgc-backend:ci
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run pytest
        run: |
          docker run --rm \
            -e DATABASE_URL=sqlite:///:memory: \
            kgc-backend:ci \
            python -m pytest tests/ -v

  frontend:
    name: Frontend (TypeScript build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Build
        working-directory: frontend
        run: npm run build

  deploy:
    name: Deploy to production
    needs: [backend, frontend]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    concurrency:
      group: production-deploy
      cancel-in-progress: false
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            set -e
            cd /opt/kinetic-time-machine
            git pull --ff-only
            docker compose -f docker-compose.prod.yml up -d --build
            docker image prune -f

  deploy_staging:
    name: Deploy to staging
    needs: [backend, frontend]
    if: github.event_name == 'push' && github.ref == 'refs/heads/staging'
    runs-on: ubuntu-latest
    concurrency:
      group: staging-deploy
      cancel-in-progress: false
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            set -e
            cd /opt/kinetic-time-machine-staging
            git fetch origin
            git checkout staging
            git pull --ff-only origin staging
            docker compose -f docker-compose.staging.yml up -d --build
            docker compose -f docker-compose.staging.yml run --rm backend python seed_teams.py
            docker compose -f docker-compose.staging.yml run --rm backend python sync_from_prod.py
            docker compose -f docker-compose.staging.yml run --rm backend python backfill_variants.py
            docker image prune -f
